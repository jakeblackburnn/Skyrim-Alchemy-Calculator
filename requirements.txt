# Core Dependencies
Django==5.2.7
numpy==2.3.3
pandas==2.3.2

# Environment variable management
python-dotenv>=1.0.0

# Production WSGI Server (recommended for deployment)
gunicorn>=21.2.0

# Static file serving for production
whitenoise>=6.6.0

# Optional: PostgreSQL adapter (if moving away from SQLite)
# psycopg2-binary>=2.9.9

jupyter>=1.0.0
